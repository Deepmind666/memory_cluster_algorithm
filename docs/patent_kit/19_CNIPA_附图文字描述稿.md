# 19 CNIPA 附图文字描述稿

## 一、用途说明
本稿用于指导绘图人员快速绘制专利附图，确保图名、标号、连线关系与说明书、权利要求书一致。  
建议最终输出 6 幅附图，并统一采用黑白线框图风格。

## 二、附图标记总表
- 100：系统总体
- 101：碎片采集模块
- 102：语义向量化模块
- 103：增量聚类模块
- 104：冲突证据图构建模块
- 105：自适应预算模块
- 106：双通道合并门控模块
- 107：语义抽取与回指解析模块
- 108：索引与检索模块
- 201-208：流程步骤 S1-S8
- 301：记忆碎片实体
- 302：语义簇实体
- 303：冲突记录实体
- 304：偏好策略实体
- 305：回溯索引实体（backrefs）
- 401：冲突值节点
- 402：值切换边
- 403：证据计数字段
- 404：冲突优先级输出
- 501：语义相似度通道
- 502：冲突兼容通道
- 503：合并允许输出
- 504：合并阻断输出
- 601：检索请求
- 602：簇摘要结果
- 603：按需回溯请求
- 604：原始证据展开结果

## 三、图1绘制文字稿（系统总体架构图）
### 图名
图1 多智能体协同记忆压缩与冲突治理系统总体架构示意图

### 版式
- 采用左到右主链路，纵向补充策略分支。
- 顶部放“多智能体输入”，底部放“存储与检索输出”。

### 组件与连接
1. 左侧输入框：“多智能体写入流”，箭头指向 `101`。
2. `101 -> 102 -> 103 -> 104 -> 105 -> 106 -> 107 -> 108` 串联主链路。
3. `104` 输出一条支线到“冲突记录输出”文本框。
4. `108` 输出两条支线：
   - 到“簇摘要输出”
   - 到“证据回溯输出”

### 图注建议
“图1示出本发明系统模块构成及主数据流向。”

## 四、图2绘制文字稿（方法流程图）
### 图名
图2 多智能体协同记忆压缩与冲突治理方法流程示意图

### 流程节点
- `201`：S1 接收并标准化记忆碎片
- `202`：S2 语义向量化与增量聚类
- `203`：S3 构建冲突证据图并计算优先级
- `204`：S4 自适应预算分配
- `205`：S5 双通道门控判定
- 判定菱形A：是否同时满足两个通道阈值
  - 是：进入 `206` 合并
  - 否：进入 `207` 阻断合并并保留冲突
- `208`：S6/S7 语义抽取、摘要生成与回溯索引保存

### 图注建议
“图2示出本发明方法的步骤顺序及关键判定分支。”

## 五、图3绘制文字稿（数据结构关系图）
### 图名
图3 记忆碎片与冲突治理相关数据结构关系示意图

### 绘制要求
1. 使用实体框表示 `301` 至 `305`。
2. `301` 与 `302`：多对一关系（多个碎片归属一个簇）。
3. `302` 与 `303`：一对多关系（一个簇可有多个冲突记录）。
4. `304` 作用于 `302`：用虚线箭头表示策略控制关系。
5. `305` 关联 `302` 与 `301`：表示摘要到原始证据的回溯路径。

### 图注建议
“图3示出本发明核心数据对象及其关联关系。”

## 六、图4绘制文字稿（冲突证据图示意图）
### 图名
图4 冲突证据图构建与优先级计算示意图

### 绘制内容
1. 左侧绘制多个 `401` 冲突值节点（例如 valueA、valueB、valueC）。
2. 节点间绘制 `402` 值切换边，边上标注切换次数。
3. 节点旁标注 `403` 证据计数。
4. 右侧输出框标注 `404` 冲突优先级结果。

### 图注建议
“图4示出冲突值关系、证据计数与优先级计算结果。”

## 七、图5绘制文字稿（双通道门控示意图）
### 图名
图5 双通道合并门控判定示意图

### 绘制内容
1. 左侧输入“候选簇对”。
2. 中间并行两个判定框：
   - `501` 语义相似度通道
   - `502` 冲突兼容通道
3. 两个判定框输出汇入“与门”符号。
4. 与门输出分两路：
   - 满足：`503` 允许合并
   - 不满足：`504` 阻断合并

### 图注建议
“图5示出语义与冲突兼容双通道联合门控逻辑。”

## 八、图6绘制文字稿（检索与回溯流程图）
### 图名
图6 基于簇摘要与回溯索引的检索展开流程示意图

### 流程
1. `601` 检索请求进入系统。
2. 返回 `602` 簇摘要结果。
3. 用户触发 `603` 按需回溯请求。
4. 输出 `604` 原始证据展开结果。

### 图注建议
“图6示出本发明从摘要检索到原始证据展开的闭环路径。”

## 九、摘要附图建议（给制图员）
- 摘要附图建议采用图1。
- 图中至少保留模块 `101` 至 `108` 及主链路箭头。
- 不放过多字段细节，保持摘要附图简洁。

## 十、制图一致性检查清单
1. 图名是否与说明书附图说明一致。
2. 图中标号是否与本稿“附图标记总表”一致。
3. 流程图步骤编号是否与 S1-S8 一一对应。
4. 双通道门控是否同时体现“允许合并”和“阻断合并”两条分支。
5. 是否体现 backrefs 回溯路径。

